# iOS 개발자 모의 면접 연습 (2026-03-23)

## Q1. [경험] 피드 화면 CPU 사용률 40% 감소 과정

### 내 답변 (보완)
- 피드 화면을 직접 사용하면서 스크롤 버벅임과 발열을 체감, 감이 아니라 데이터로 확인하기 위해 **Xcode Instruments(Time Profiler)로 프로파일링**하고 최적화 전후 CPU 사용률을 비교해서 **40% 감소**를 수치로 확인
- **메인 스레드 과부하 해소:** 메인 스레드에서 돌 필요 없는 무거운 연산(비디오 디코딩, 데이터 파싱 등)을 백그라운드 스레드로 이동, 셀 높이 등 반복 계산되는 값은 메모리 캐싱으로 재계산 제거
- **레이아웃 최적화:** 기존 UICollectionView + 중첩 스크롤뷰 구조에서 **CompositionalLayout으로 전환** → 섹션별 독립 레이아웃을 선언적으로 정의하여 **뷰 계층 평탄화**, 중첩 레이아웃으로 인한 불필요한 layout pass 제거
- **미디어 처리:** 동영상은 HLS 스트리밍 적용 + 로딩 중 검은 화면 대신 썸네일 먼저 표시, 이미지 캐시는 검증된 라이브러리(Kingfisher) 사용, PDF는 FileManager 기반 디스크 캐싱 직접 구현
- **사용자 관점 결과:** 스크롤 시 hitch 제거, 사용자로부터 들어오던 발열 소멸

---

## Q2. [기술] GCD vs OperationQueue vs Swift Concurrency 차이점

### 내 답변 (보완)
- **GCD:** 코드 블록을 원하는 큐(Serial/Concurrent)에서 실행, 단순하고 직관적. 다만 콜백이 중첩되면 가독성이 떨어지고, 에러 전파가 어려운 단점이 있음
- **OperationQueue:** 작업을 Operation 객체로 캡슐화하여 `maxConcurrentOperationCount`로 동시 실행 수 제한, `cancel()`로 취소, `addDependency`로 작업 간 실행 순서 보장 등 세밀한 제어가 가능
- **Swift Concurrency:** async/await로 비동기 코드를 동기처럼 작성, try-catch로 에러 처리 간편. **Structured Concurrency**(TaskGroup)로 자식 태스크의 생명주기가 부모 스코프에 바인딩되어 누수 방지. **Actor**로 데이터 레이스를 컴파일 타임에 방지할 수 있어 동시성 안전성이 크게 향상
- 실무에서는 주로 ViewModel에서 API 호출 시 Swift Concurrency를 사용하고, 이미지 다운로드처럼 세밀한 큐 제어가 필요한 경우 GCD/OperationQueue를 병행

---

## Q3. [경험+기술] Atomic Design 디자인 시스템 + SPM 배포

### 내 답변 (보완)
- **Atomic Design 위계:** Atom(색상, 폰트, 아이콘 — 나눌 수 없는 최소 단위) → Molecule(버튼, 텍스트필드, 스낵바 — Atom 조합, 재사용 빈도 높은 컴포넌트) → Organism(리스트 아이템, 네비게이션바 — Molecule 조합)
- **SPM 배포:** 각 위계를 SPM 패키지로 분리하여 의존성 방향을 명확히 관리. 다만 디자인 시스템 수정 시 별도 SPM 프로젝트로 전환해야 해서 컨텍스트 스위칭 비용이 발생
- **생산성 문제 해결:** 이후 Cre8orClub에서는 Tuist 모듈화를 적용해 디자인 시스템을 로컬 모듈로 관리, 하나의 프로젝트 안에서 수정과 확인이 가능하도록 개선. 다시 한다면 개발 중에는 로컬 SPM으로 빠르게 이터레이션하고, 안정화 후 리모트 SPM으로 전환하는 방식을 택할 것
- **30% 단축 근거:** 디자인 시스템 도입 전 신규 화면 개발 시 색상·폰트·버튼 등을 매번 개별 정의하던 것이, 도입 후 Foundation + Component 조합만으로 UI를 구성할 수 있게 되어 기존 약 3일 소요되던 화면 작업이 2일 내외로 단축
- **디자이너 협업:** 디자이너와 Figma 컴포넌트 네이밍을 코드 네이밍과 1:1로 싱크하여, 디자인 핸드오프 시 "이 컴포넌트가 코드에서 어디에 해당하는지" 커뮤니케이션 비용을 최소화

---

## Q4. [기술 심화] Tuist 모듈화 아키텍처 설계

### 내 답변 (보완)
- **모듈 구성:** Domain(비즈니스 로직, 엔티티), Data(네트워크 통신, Repository 구현체), Feature(큰 기능 단위 화면/ViewModel), App(앱 진입점, DI 조립)
- **의존성 방향 규칙:** App → Feature → Data → Domain. **Domain은 어디에도 의존하지 않는 순수 비즈니스 레이어**
- **Feature가 Data를 아는 이유:** Feature가 Data를 직접 import해야 DI 컨테이너에 구체 타입을 **컴파일 타임에 등록**할 수 있음. Feature가 Domain만 안다면 구체 구현체를 런타임에 등록해야 하므로, 타입 미스매치를 실행 시점에야 발견하게 되는 위험이 있음. 컴파일 타임 타입 안전성을 우선한 설계 판단
- **설계 원칙:** ① Domain에 외부 의존성(서드파티 라이브러리) 금지 ② Feature 간 직접 참조 금지 ③ Data → Domain 방향만 허용
- **Feature 간 통신:** Feature 모듈끼리 직접 import하지 않고, 공통 Common 모듈에 Combine의 PassthroughSubject 기반 이벤트 스트림을 정의하여 통신. NotificationCenter 대비 타입 안전성이 보장되고, AnyCancellable로 구독 생명주기를 명시적으로 관리할 수 있어 메모리 누수 위험도 낮음
- **순환 의존성 방지:** Tuist generate 시점에 순환 의존을 컴파일 에러로 감지해주기 때문에 설계 규칙 위반을 빌드 단계에서 차단할 수 있음
- 일정과 실익을 중요시한 판단

---

## Q5. [행동/협업] 기획자/디자이너와 의견 충돌 경험

### 내 답변 (보완)
- 사용자 경험 측면: 기획자/디자이너가 더 깊이 고려한 경우가 많아서, 배경 설명을 듣고 수용
- 기술적 난이도 측면: 대안을 제시 (더 빠른 구현, 안정적 경험 등 상대방 관점의 이점으로 설득)
- 임팩트가 크다면 기꺼이 기술적 챌린지 수용
- **구체적 사례 (STAR):**
  - **Situation:** 캐러셀 UI 구현 요청이 들어옴
  - **Task:** 커스텀 캐러셀을 직접 구현하는 방향으로 진행될 예정이었음
  - **Action:** SwiftUI TabView의 `.page` 스타일을 대안으로 제시. 시스템 네이티브 UI라 안정성이 검증되어 있고, 구현 비용이 낮아 다른 작업에 리소스를 쓸 수 있다는 점을 상대방 관점의 이점으로 설득
  - **Result:** 대안이 수용되어 빠르게 구현 완료, 절약된 리소스를 다른 우선순위 작업에 투입

---

## Q6. [기술] Combine vs RxSwift 차이점과 선택 기준

### 내 답변 (보완)
- **입장:** 새 프로젝트라면 Combine을 선택. 외부 의존성 없이 Apple 자체 지원이므로 장기 유지보수 부담이 적음
- **Combine:** Swift 내장, 버전 관리 부담 없음, SwiftUI와 필수 조합(ObservableObject). 다만 RxSwift 대비 연산자 수가 적은 편
- **RxSwift:** RxCocoa로 UIKit 바인딩이 편리하고, Driver/Signal trait으로 UI 스레드 보장 + 에러 미방출 등 UI에 특화된 안전장치 제공. 연산자가 풍부해서 복잡한 스트림 조합에 유리
- **Observation 프레임워크(iOS 17):** `@Observable` 매크로로 Combine 없이도 상태 관리 가능. 프로퍼티 레벨 관찰이라 불필요한 뷰 리렌더링이 줄어듦. Apple이 SwiftUI의 미래 방향으로 밀고 있어서, 최소 지원 버전이 iOS 17 이상이라면 ViewModel 상태 관리는 Observation으로 전환하는 것이 합리적

---

## Q7. [문제 해결] 프로덕션 간헐적 크래시 디버깅 접근법

### 내 답변 (보완)
- **1단계 — 크래시 리포트 분석:** Sentry/Crashlytics에서 스택 트레이스 확인, dSYM 심볼리케이션이 정상 적용되었는지 점검하여 정확한 크래시 지점 파악
- **2단계 — 패턴 파악:** OS 버전, 디바이스, 발생 빈도, 진입 경로 등을 기준으로 공통 조건 분석. 특정 OS나 특정 화면 진입 경로에서만 발생하는지 확인
- **3단계 — 코드 분석:** 크래시 지점 주변의 force unwrap, 동시성 이슈(race condition), 메모리 해제 타이밍(댕글링 포인터) 등 의심 원인 코드 리뷰
- **4단계 — 재현 시도:** Release 빌드로 재현, Thread Sanitizer(동시성 이슈)나 Address Sanitizer(메모리 이슈)를 활용하여 런타임에서 문제 검출. 특정 유저 문제라면 동일한 데이터/상태값으로 환경 재현
- **5단계 — 수정 및 배포:** 방어 코드 + 의심 지점에 로깅 추가 후 핫픽스 배포, 이후 크래시율 모니터링으로 해결 여부 확인

---

## Q8. [설계] 여러 화면의 유저 프로필 데이터 동기화

### 내 답변 (보완)
- 기존에는 각 화면에서 API를 호출해 서버 정합성을 맞추는 방식을 사용했지만, 이 방식은 네트워크 비용 증가, 로딩 UX 저하, 오프라인 대응 불가 등의 문제가 있음
- **권장하는 접근법 — Single Source of Truth 패턴:**
  - `UserProfileRepository`가 유일한 프로필 데이터 소유자 역할
  - Combine의 `CurrentValueSubject`로 데이터를 발행하고, 각 화면의 ViewModel이 구독하여 리액티브하게 동기화
  - 서버 동기화 시점은 앱 진입 시, pull-to-refresh, 프로필 수정 직후 등 명확한 트리거로 제한
  - **Optimistic Update:** 프로필 수정 시 API 응답을 기다리지 않고 로컬 먼저 갱신하여 즉각적인 UX 제공, 실패 시 롤백
- 이 구조라면 화면이 늘어나도 Repository 구독만 추가하면 되므로 확장성도 확보됨

---

## Q9. [기술] Universal Link vs URL Scheme

### 내 답변 (보완)
- **URL Scheme:** 커스텀 스킴(`myapp://`)을 등록하여 앱을 오픈. 단, 여러 앱이 동일한 scheme을 등록할 수 있어 hijacking 위험이 존재하고, 앱 미설치 시 에러만 발생
- **Universal Link:** 웹 URL 기반으로, 앱 설치 시 앱이 열리고 미설치 시 웹 fallback. 도메인 소유권을 검증하므로 보안이 우수하며 실무에서는 Universal Link를 우선 사용
- **구현 시 주의할 점 (실무 경험 기반):**
  - **AASA 파일:** `/.well-known/apple-app-site-association`을 서버에 호스팅, Content-Type `application/json` 필수
  - **Apple CDN 캐싱:** Apple이 AASA를 자체 CDN에 캐싱하므로 업데이트 반영이 즉시 안 됨 → 디버깅 시 혼란 유발
  - **설치 시점 다운로드:** AASA는 앱 설치/업데이트 시점에 다운로드되므로, 서버에서 수정해도 기존 유저에게 바로 반영되지 않음
  - **같은 도메인 제한:** Safari에서 현재 페이지와 같은 도메인의 Universal Link 클릭 시 앱이 열리지 않음 (cross-domain에서만 동작)
- 실무에서 Universal Link + AppsFlyer OneLink를 조합하여 마케팅/푸시/웹 등 다양한 진입점에서 일관된 딥링크 경험을 구현한 경험이 있음

---

## Q10. [가치관] 가장 성장한 순간 + 되고 싶은 개발자

### 내 답변 (보완)
- 기획 회의에서 "기술적으로 구현 가능한가?" → "비즈니스 임팩트가 뭔가?"로 사고가 전환된 순간이 가장 성장한 때
- 과거에는 코드를 잘 짜는 개발자가 목표였지만, AI 시대에는 코드 작성 자체보다 **비즈니스 맥락에서의 의사결정**이 개발자의 핵심 역할이라고 생각
- AI를 적극 활용하되, "무엇을 만들어야 하는가"와 "왜 이 구조여야 하는가"를 판단하는 것은 개발자의 몫
- **구체적 사례:** 공유 기능 기획 회의에서 단순 링크 복사가 아니라 브랜디드 도메인 기반 Universal Link를 제안. "공유 링크가 브랜드 URL이면 클릭률이 올라가고, 앱 미설치 유저도 웹으로 유입된다"는 비즈니스 관점으로 설득하여 채택. 기술적 구현뿐 아니라 사용자 유입 경로까지 고려한 제안이 서비스에 기여한 경험
- 서비스를 같이 고민하고, 기술로 비즈니스 임팩트에 긍정적 영향을 줄 수 있는 개발자가 되고 싶음

